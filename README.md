# Sui DeepBook Market Maker Bot (starter)

## What this repo does
- Connects to Sui (testnet/mainnet)
- Initializes DeepBookV3 TypeScript SDK client
- Prints a Level2 snapshot for a pool key

## Quick start (Codespaces)
1. Create a repo with these files
2. Open in Codespaces
3. Copy `.env.example` → `.env`
4. Put a DEV wallet private key into `SUI_PRIVATE_KEY`
5. Run:

```bash
pnpm start
