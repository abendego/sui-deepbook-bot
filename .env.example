# Network
SUI_ENV=testnet  # testnet | mainnet

# Private key in Sui format (be careful—use a dev wallet first)
SUI_PRIVATE_KEY=

# DeepBook pool key (SDK ships common defaults; see Sui docs)
POOL_KEY=SUI_DBUSDC

# L2 query params
L2_TICK_SIZE=0.1
L2_LEVELS=50
L2_INCLUDE_ASKS=true

# Safety
ALLOW_TRADING=false
MAX_ORDER_USD=2
ORDER_SIZE_BASE=0.1

# DeepBook / assets
POOL_KEY=SUI_DBUSDC
BASE_COIN=SUI
QUOTE_COIN=DBUSDC

# Optional: override if needed
# BALANCE_MANAGER_ID=
