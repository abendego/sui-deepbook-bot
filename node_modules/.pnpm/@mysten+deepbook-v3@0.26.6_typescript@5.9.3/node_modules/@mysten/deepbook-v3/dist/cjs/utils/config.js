"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var config_exports = {};
__export(config_exports, {
  DEEP_SCALAR: () => DEEP_SCALAR,
  DeepBookConfig: () => DeepBookConfig,
  FLOAT_SCALAR: () => FLOAT_SCALAR,
  GAS_BUDGET: () => GAS_BUDGET,
  MAX_TIMESTAMP: () => MAX_TIMESTAMP,
  POOL_CREATION_FEE_DEEP: () => POOL_CREATION_FEE_DEEP,
  PRICE_INFO_OBJECT_MAX_AGE_MS: () => PRICE_INFO_OBJECT_MAX_AGE_MS
});
module.exports = __toCommonJS(config_exports);
var import_utils = require("@mysten/sui/utils");
var import_balanceManager = require("../transactions/balanceManager.js");
var import_errors = require("./errors.js");
var import_constants = require("./constants.js");
var _coins, _pools, _marginPools;
const FLOAT_SCALAR = 1e9;
const DEEP_SCALAR = 1e6;
const MAX_TIMESTAMP = 1844674407370955161n;
const PRICE_INFO_OBJECT_MAX_AGE_MS = 5e3;
const GAS_BUDGET = 25e7;
const POOL_CREATION_FEE_DEEP = 5e8;
class DeepBookConfig {
  constructor({
    env,
    address,
    adminCap,
    marginAdminCap,
    marginMaintainerCap,
    balanceManagers,
    marginManagers,
    coins,
    pools,
    marginPools
  }) {
    __privateAdd(this, _coins);
    __privateAdd(this, _pools);
    __privateAdd(this, _marginPools);
    this.env = env;
    this.address = (0, import_utils.normalizeSuiAddress)(address);
    this.adminCap = adminCap;
    this.marginAdminCap = marginAdminCap;
    this.marginMaintainerCap = marginMaintainerCap;
    this.balanceManagers = balanceManagers || {};
    this.marginManagers = marginManagers || {};
    if (env === "mainnet") {
      __privateSet(this, _coins, coins || import_constants.mainnetCoins);
      __privateSet(this, _pools, pools || import_constants.mainnetPools);
      __privateSet(this, _marginPools, marginPools || import_constants.mainnetMarginPools);
      this.DEEPBOOK_PACKAGE_ID = import_constants.mainnetPackageIds.DEEPBOOK_PACKAGE_ID;
      this.REGISTRY_ID = import_constants.mainnetPackageIds.REGISTRY_ID;
      this.DEEP_TREASURY_ID = import_constants.mainnetPackageIds.DEEP_TREASURY_ID;
      this.MARGIN_PACKAGE_ID = import_constants.mainnetPackageIds.MARGIN_PACKAGE_ID;
      this.MARGIN_REGISTRY_ID = import_constants.mainnetPackageIds.MARGIN_REGISTRY_ID;
      this.LIQUIDATION_PACKAGE_ID = import_constants.mainnetPackageIds.LIQUIDATION_PACKAGE_ID;
      this.pyth = import_constants.mainnetPythConfigs;
    } else {
      __privateSet(this, _coins, coins || import_constants.testnetCoins);
      __privateSet(this, _pools, pools || import_constants.testnetPools);
      __privateSet(this, _marginPools, marginPools || import_constants.testnetMarginPools);
      this.DEEPBOOK_PACKAGE_ID = import_constants.testnetPackageIds.DEEPBOOK_PACKAGE_ID;
      this.REGISTRY_ID = import_constants.testnetPackageIds.REGISTRY_ID;
      this.DEEP_TREASURY_ID = import_constants.testnetPackageIds.DEEP_TREASURY_ID;
      this.MARGIN_PACKAGE_ID = import_constants.testnetPackageIds.MARGIN_PACKAGE_ID;
      this.MARGIN_REGISTRY_ID = import_constants.testnetPackageIds.MARGIN_REGISTRY_ID;
      this.LIQUIDATION_PACKAGE_ID = import_constants.testnetPackageIds.LIQUIDATION_PACKAGE_ID;
      this.pyth = import_constants.testnetPythConfigs;
    }
    this.balanceManager = new import_balanceManager.BalanceManagerContract(this);
  }
  // Getters
  getCoin(key) {
    const coin = __privateGet(this, _coins)[key];
    if (!coin) {
      throw new import_errors.ResourceNotFoundError("Coin", key);
    }
    return coin;
  }
  getPool(key) {
    const pool = __privateGet(this, _pools)[key];
    if (!pool) {
      throw new import_errors.ResourceNotFoundError("Pool", key);
    }
    return pool;
  }
  getMarginPool(key) {
    const pool = __privateGet(this, _marginPools)[key];
    if (!pool) {
      throw new import_errors.ResourceNotFoundError("Margin pool", key);
    }
    return pool;
  }
  /**
   * @description Get the balance manager by key
   * @param managerKey Key of the balance manager
   * @returns The BalanceManager object
   */
  getBalanceManager(managerKey) {
    if (!Object.hasOwn(this.balanceManagers, managerKey)) {
      throw new Error(import_errors.ErrorMessages.BALANCE_MANAGER_NOT_FOUND(managerKey));
    }
    return this.balanceManagers[managerKey];
  }
  /**
   * @description Get the margin manager by key
   * @param managerKey Key of the margin manager
   * @returns The MarginManager object
   */
  getMarginManager(managerKey) {
    if (!Object.hasOwn(this.marginManagers, managerKey)) {
      throw new Error(import_errors.ErrorMessages.MARGIN_MANAGER_NOT_FOUND(managerKey));
    }
    return this.marginManagers[managerKey];
  }
}
_coins = new WeakMap();
_pools = new WeakMap();
_marginPools = new WeakMap();
//# sourceMappingURL=config.js.map
